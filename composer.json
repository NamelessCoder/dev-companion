{
    "name": "typo3/cms-mcp",
    "description": "Local stdio MCP server exposing a curated TYPO3 core contribution knowledge base.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.2",
        "mcp/sdk": "^0.7.0"
    },
    "autoload": {
        "psr-4": {
            "Typo3CmsMcp\\": "src/"
        }
    },
    "bin": [
        "bin/typo3-cms-mcp"
    ],
    "config": {
        "sort-packages": true
    }
}
