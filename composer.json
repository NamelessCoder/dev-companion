{
    "name": "typo3/dev-companion",
    "description": "Local MCP server that guides coding agents through TYPO3 implementation, review and verification with version-bound knowledge and project facts.",
    "type": "library",
    "license": "MIT",
    "support": {
        "source": "https://github.com/benjaminkott/typo3-dev-companion",
        "issues": "https://github.com/benjaminkott/typo3-dev-companion/issues",
        "docs": "https://benjaminkott.github.io/typo3-dev-companion/"
    },
    "require": {
        "php": ">=8.2",
        "ext-curl": "*",
        "ext-dom": "*",
        "mcp/sdk": "^0.7.0",
        "symfony/finder": "^7.4",
        "symfony/yaml": "^7.4"
    },
    "autoload": {
        "psr-4": {
            "TYPO3\\DevCompanion\\": "src/"
        }
    },
    "bin": [
        "bin/typo3-dev-companion"
    ],
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true
        }
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.95",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^11.5",
        "symfony/console": "^7.4"
    },
    "autoload-dev": {
        "psr-4": {
            "TYPO3\\DevCompanion\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "lint": [
            "find bin -type f -print0 | xargs -0 -n1 php -l > /dev/null",
            "find src tests -name '*.php' -print0 | xargs -0 -n1 php -l > /dev/null"
        ],
        "cgl": "php-cs-fixer fix --diff",
        "cgl:ci": "php-cs-fixer fix --diff --dry-run",
        "stan": "phpstan analyse",
        "test": "phpunit",
        "ci": [
            "@lint",
            "@cgl:ci",
            "@stan",
            "@test"
        ]
    },
    "scripts-descriptions": {
        "lint": "Check every PHP file for syntax errors.",
        "cgl": "Bring every PHP file to the coding guidelines.",
        "cgl:ci": "Report what cgl would change, and change nothing.",
        "stan": "Run the static analysis.",
        "test": "Run the test suite.",
        "ci": "Everything CI runs: lint, coding guidelines, static analysis, tests."
    }
}
