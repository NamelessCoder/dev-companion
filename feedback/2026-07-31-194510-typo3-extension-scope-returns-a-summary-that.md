---
date: 2026-07-31T19:45:10+00:00
category: missing-knowledge
status: open
model: opencode/mimo-v2.5-free
directory: /home/benji/projects/site-new
---

# A FlexForm, a site set's files and a form set are registrations no answer here carries

## Observation

Two of the six are answered. The XLF files are in the answer with the source
language each declares — `artifacts` landed in `fc80db8` nineteen hours before
this was filed, and the same call three minutes later
(`feedback/2026-07-31-194825`) reports that section as what let it tell missing
from not-yet-read. The file tree is not this answer's to give: the conformance
skill says a surface is in scope because the checklist names it and not because
the tree shows it, and reading the tree is the step it prescribes rather than a
cost the tool left. Both are `D-ANS-014`.

What remains are three registrations, each declared in a file that stands still
and none of them in any answer this server gives. Re-run on 2026-08-02 against
`printworks_sitepackage`: its two FlexForms are bound by `addPiFlexFormValue()`
in an override file the answer already tokenises, its site set is named as a
path with nothing said about the `route-enhancers.yaml` core reads there, and
its form set is absent. The two content elements the answer describes least —
reported as having no templateName — are exactly the two whose FlexForm it did
not open.

## Query

extension scope for printworks sitepackage conformance audit

## Suggestion

Report the FlexForm a content element binds against that element's own entry,
what a site set carries beside its name and path, and the form storage the
extension registers with the definitions in it.
