---
date: 2026-08-03T14:44:10+00:00
category: idea
status: open
model: claude-opus-5[1m]
tool: typo3_task_guide, typo3_hint_lookup, typo3-core-patch-review
directory: /home/benji/projects/typo3-cms
---

# Task: review core commit 9f6c6eb9093 (#110359) and report what blocks it.

## Observation

Trimmed on 2026-08-03 to the part that is left. Both observations about hints
are answered. The first — that `typo3_task_guide` subsumes `typo3_hint_lookup`,
so the skill's per-subsystem step asks for a re-fetch — does not hold.
`D-GUI-007` decided which of the two tools owns hint delivery on 2026-08-03,
from this same run, and shipped what this feedback proposed as its own option
(b): the brief states that its hints are `typo3_hint_lookup`'s. Re-run on
2026-08-03 with the same five paths, the same task text and the same
`targetVersion`, the lookup answers `fal-reading`, `fal-processing` and
`dependency-injection` beyond the four the brief carried. The separate call was
not a re-fetch, and the dependency-injection hint this report says it lost was
one call away.

The second is the ask underneath it: the brief said how many hints it carries
and never which ones it left, so a subsystem it did not reach was invisible
until the call was made. That is `R-GUI-012`, held on 2026-08-03 by
`HintsTest::aBriefNamesTheHintsItLeftBehind` — those three ids are named under
the four bodies and in `omittedHints`, so what this session went to the checkout
for (`#[Autowire(lazy: true)]`, grepped out of three call sites) is one call
away and says so.

Still open, unchanged — a small schema wart. I passed `area` and `paths`
separately. The answer folded the area string into the paths array as a sixth
element and scoped it: `{"path":"core Resource / extbase Service / fluid
ViewHelpers","scope":"core"}`. A free-text area description was treated as a
file path and given a scope verdict. Harmless here, but it means the `scopes`
array cannot be read as "these are the files" by anything downstream. That is
the second **Wrong if** of `D-SCO-009`, which names deprecating `area` as its
own repair where this one names marking it; both hold, so the question is with
whoever maintains the repository.

## Query

typo3_task_guide(task "Review a core patch that moves file source resolution from Extbase ImageService into ResourceFactory and adds a new public method", area "core Resource / extbase Service / fluid ViewHelpers", changeType "audit", targetVersion "15.0", paths = [ResourceFactory.php, ImageService.php, fluid ImageViewHelper.php, backend ThumbnailViewHelper.php, core Tests/Functional/Resource/ResourceFactoryTest.php]). typo3_hint_lookup was never called separately in this session.

## Suggestion

Keep `area` out of the `scopes` array, or mark it `{"kind":"area"}` rather than
scoping it as a path.
