[
  {
    "id": "backend-modules",
    "title": "Backend Module and Route Registration",
    "domains": [
      "php"
    ],
    "appliesTo": [
      "Configuration/Backend/Modules.php",
      "Configuration/Backend/Routes.php",
      "Configuration/Backend/AjaxRoutes.php",
      "backend module",
      "module registration",
      "doc header",
      "button bar",
      "shortcut button",
      "setShortcutContext",
      "POST redirect",
      "redirect after POST",
      "ModuleTemplate",
      "moduleTemplateFactory",
      "module controller",
      "moduleData",
      "renderResponse"
    ],
    "hints": [
      "A backend module is declared in the owning extension's Configuration/Backend/Modules.php, which returns an array keyed by module identifier. There is no registration call.",
      "The keys of an entry are parent (the identifier of the module it sits under), access (\"user\", \"admin\", ...), path (the backend route it answers on), iconIdentifier, labels, aliases (former identifiers kept working), routes, and moduleData for the module's persisted per-user state.",
      "routes maps a route name to a target of the form Controller::class . '::method'; the entry named _default is what the module opens with.",
      {
        "text": "labels is a translation domain, not a file path. typo3_translation_domain_lookup computes it for the XLF file the module's labels live in.",
        "since": 14
      },
      {
        "text": "labels is the LLL:EXT: reference of the XLF file the module's labels live in, not a domain.",
        "until": 13
      },
      {
        "text": "Configure a module shortcut with $moduleTemplate->getDocHeaderComponent()->setShortcutContext($routeIdentifier, $displayName, $arguments). The doc header creates and positions the ShortcutButton; do not add one to its button bar manually.",
        "since": 14
      },
      {
        "text": "Build a module shortcut through $moduleTemplate->getDocHeaderComponent()->getButtonBar()->makeShortcutButton(), set its route identifier, display name and arguments, then add it to that button bar.",
        "until": 13
      },
      "After a module POST changes state, return a RedirectResponse with HTTP 303 status. The browser then follows with GET; HTTP 302 does not state that method change and can repeat the POST.",
      "Configuration/Backend/Routes.php and AjaxRoutes.php declare backend routes outside a module, in the same declarative style.",
      "A module's controller answers with a response the module template renders: $this->moduleTemplateFactory->create($request), the variables assigned to it, then $view->renderResponse('<Directory>/<Template>') for the Fluid template below Resources/Private/Templates. It is the doc header, the module menu and the backend chrome the module sits in, so a response built any other way is a page outside the backend.",
      "The module's persisted per-user state arrives on the request as the moduleData attribute, built from the moduleData key of the registration. set() changes the object alone: what writes it back for the next request is $backendUser->pushModuleData($moduleData->getModuleIdentifier(), $moduleData->toArray()), and a module that never calls it looks like one whose state is not persisted at all.",
      "These are declarative files with no schema check behind them: a wrong key does not fail at boot, it fails when a user opens the module. Take the shape from a neighbouring extension."
    ]
  },
  {
    "id": "backend-routing-internals",
    "title": "Backend Route Options, Sub-Route Identifiers, and Navigation Inheritance",
    "domains": [
      "php"
    ],
    "appliesTo": [
      "Classes/Routing/",
      "Classes/Module/",
      "Classes/Breadcrumb/",
      "route identifier",
      "sub-route",
      "subroute",
      "module option",
      "getOption",
      "ModuleRegistry",
      "ModuleProvider",
      "ModuleResolver",
      "ExtbaseModule",
      "navigationComponent",
      "page tree navigation",
      "breadcrumb",
      "which module a request is in"
    ],
    "hints": [
      "A route a module registers carries the module object in its module option; a route declared in Configuration/Backend/Routes.php or AjaxRoutes.php does not. Module::getDefaultRouteOptions() puts it on every route of the module, so that option is what says whether a route belongs to a module at all.",
      {
        "text": "ModuleResolver is what answers which module a request is in, and it reads three sources in order: the module option of the routed route, the module request attribute, then the module query parameter.",
        "since": 14
      },
      "The module's _default route is registered under the module identifier itself. Every other route of the module is registered as <module identifier>.<route name>, because ModuleRegistry adds the collection under the module identifier and a dot.",
      "An Extbase module generates one route per controller action rather than taking a routes map, named <ControllerAlias>_<action>, so its sub-route identifiers read <module identifier>.<ControllerAlias>_<action>. Every one of them gets the module's own route options; there is no per-action entry to configure.",
      "navigationComponent is inherited from the parent module unless the registration turns it off with inheritNavigationComponentFromMainModule, so a module's own Modules.php does not say whether it is page-tree navigated. Reading the registration files alone gives the wrong answer for every module that inherits.",
      "TYPO3\\CMS\\Backend\\Routing\\Route takes a path and an options array; TYPO3\\CMS\\Core\\Routing\\Route takes a path, defaults, requirements and then options. Passing backend route options to the core class puts them where defaults belong, getOption() returns null with no error, and a test built that way asserts nothing."
    ]
  }
]
