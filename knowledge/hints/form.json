[
  {
    "id": "form-framework",
    "title": "EXT:form Configuration and Runtime",
    "domains": [
      "php",
      "fluid"
    ],
    "appliesTo": [
      "EXT:form",
      "form framework",
      "form definition",
      ".form.yaml",
      "Configuration/Form/",
      "allowedExtensionPaths",
      "prefill form",
      "prefill field",
      "AfterCurrentPageIsResolvedEvent",
      "afterFormStateInitialized"
    ],
    "hints": [
      {
        "text": "An extension's form setup is auto-discovered from Configuration/Form/<SetName>/config.yaml. The file carries name, label and priority plus the setup itself; the core base set typo3/form-base deliberately has a lower priority than an extension set's default. No TypoScript registration is needed.",
        "since": 14
      },
      {
        "text": "A setup path is registered under TypoScript, and twice: under plugin.tx_form.settings.yamlConfigurations and under module.tx_form.settings.yamlConfigurations.",
        "until": 14
      },
      {
        "text": "That TypoScript registration is the predecessor of form sets and is still read: a path found under either key is merged after the auto-discovered ones, and reading it raises an E_USER_DEPRECATED. The core itself no longer declares the key.",
        "since": 14,
        "until": 14
      },
      "A version-controlled definition shipped by an extension lives below a path listed in persistenceManager.allowedExtensionPaths — conventionally Resources/Private/Forms/ — and its filename ends in .form.yaml. Database records are for editable definitions; file-mount storage is the predecessor, not a replacement for read-only extension storage.",
      "In the frontend FormFrontendController appends the content-element uid to the declared form identifier and retains the declared value in renderingOptions._originalIdentifier. Match the latter when code must identify the form; matching FormDefinition::getIdentifier() against the YAML value works in an isolated test and misses at runtime.",
      {
        "text": "To prefill from the current request, listen to AfterCurrentPageIsResolvedEvent: it carries the FormRuntime and the request and is dispatched after request state was initialized but before submitted values are processed. Set an element's default value there, so a submitted value still wins. Form variants expose form values and root-form properties, not arbitrary request values, and cannot carry a dynamic value into a field.",
        "since": 14
      },
      "Two EXT:form SC_OPTIONS hooks are still called: ext/form/afterFormStateInitialized changes the runtime after request state is loaded, and ext/form/buildFormDefinitionValidationConfiguration changes the validation rules while the definition is built. Their existence is not a reason to choose them by default: use the narrower PSR-14 event where one exposes the payload the task needs.",
      "plugin.tx_form.settings.formDefinitionOverrides is keyed by the declared, unsuffixed identifier. It is merged with ArrayUtility::mergeRecursiveWithOverrule, so nested maps such as finisher recipients are merged unless their keys are explicitly replaced."
    ]
  }
]
