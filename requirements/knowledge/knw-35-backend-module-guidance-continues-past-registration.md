---
id: R-KNW-35
status: held
---

# R-KNW-35 — Backend-module guidance continues past registration

**Backend-module guidance continues past registration.**

It names the version-bound shortcut API used by the doc header and requires a
303 redirect after a state-changing POST so the browser follows with GET.

## From

A module that manually added the superseded shortcut button and answered its
import POST with a redirect that could repeat it (2026-07-30).

## Held by

- `HintsTest::aBackendModuleNamesItsShortcutApiAndPostRedirect`
