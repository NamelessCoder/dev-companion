# Hand over one feedback per run and let `decisions/` carry the journal

**Serves:** decisions/, documentation/

Answered on 2026-08-02, which is what takes this out of `waiting/`: the portion
goes to **one**, and there is **no journal file**. What a run decided is written
where that kind of thing already lives — the decision it judged against is
updated, and where the judgement establishes something no entry says yet, a new
one is created. So the cross-feedback view a portion of five gave for free is
not lost to a second list nobody keeps in step with the archive; it is
`decisions/`, read the way it already is. Build the command half:
`bin/cli feedback next` hands over the oldest feedback with no answer and exits
nonzero while any remain, and the **skip** lasts one pass and is written down
nowhere. Then correct `D-FBK-5` in place — its own **Wrong if** is what this
answers — bring `R-FBK-7` to the shape that is actually held, and take the draft
header off
[judging.md](../documentation/feedback/judging.md), which says out loud that the
page and the command disagree until this is answered. The recurring todo is the
fourth place the number is written down — `run-the-next-five-open-feedback-s-queries-against-the`
says five in its title, its paragraph and its `**Run:**` line, and a session
reads that rather than the decision.
