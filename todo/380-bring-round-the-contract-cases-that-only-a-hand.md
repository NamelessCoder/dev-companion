# Bring round the contract cases that only a hand reading holds

**Serves:** decisions/

Seven contract cases fall back on being read by hand where their **Held by**
line says `not guarded` — `CORE-04`, `CORE-06`, `EXT-01`, `SITE-01`, `SITE-08`,
`SKILL-07` and `SKILL-09` — and nothing brings that reading round: `todo/recurring/`
holds four todos and none of them names `scenarios/contracts/`. `D-EVI-2` already
rests on `SKILL-07` read by hand, and `SKILL-09` says it is what measures the
rest, so two of the seven carry weight nobody has been back to. The step is to
settle the cadence and write the recurring todo that reads them, or to say in
each case why no schedule is owed — `D-EVI-1`'s reading of 2026-08-02 is what
found this and holds the other half.
