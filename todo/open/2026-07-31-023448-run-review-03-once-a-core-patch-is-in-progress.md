# Record and judge the `REVIEW-03` run

**Serves:** REVIEW-03
**Priority:** normal

`REVIEW-03` is the one forward review of the three that has never run, and what
blocked it was a state over there rather than a fix here. That is answered:
`E-CORE` is `/home/benji/projects/typo3-cms`, it is worked in and carries
unpushed core patches without one being made for the review, and its skills were
republished from this checkout on 2026-08-03 —
`todo/reference/which-checkout-plays-which-environment.md` carries the entry. The
session is driven there rather than from here. `bin/cli scenarios:record
REVIEW-03 <client>` writes the empty run before it starts, the prompt from
`bin/cli scenarios:show REVIEW-03` goes in verbatim, and
`documentation/evidence/forward-runs.md` is the rest of the procedure. What is
left here is the judgement: the five criteria, the skills that activated, and the
tool calls with the arguments they were made with, all read from the transcript
rather than from what the session says about itself. The GD/SVG review in the
same checkout on 2026-08-01 is not that run — it was given a subsystem task
instead of the prompt.
