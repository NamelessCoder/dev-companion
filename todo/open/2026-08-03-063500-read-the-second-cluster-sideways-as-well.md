# Read the second cluster sideways as well

**Serves:** feedback/, decisions/
**Priority:** normal

The reading
[`D-FBK-025`](../../decisions/feedback/fbk-025-a-judgement-reads-the-corpus-decides-the-shape-and-sets-the-priority.md)
prescribes was run for the core checkout and turned 35 feedback into two skills;
the other group `bin/cli feedback:list` prints has never had it. Eighteen open
feedback carry `directory: /home/benji/projects/site-new`, and each was judged
alone into a `low` card about one statement — the Extbase fork, which page may
hold a record, what a functional test's database holds, which caches a change
invalidates, what a preview owes the editor, where a one-off script may not go.
Read the eighteen together and decide whether they are eighteen statements or
one shape: they are one session building a content element in a sitepackage, and
what it lost was the order rather than any single fact, which is also what the
three cards in `todo/waiting/` asking whether `typo3_task_guide` names the skill
that owns a task are about. The outcome is a judgement written into `decisions/`
— either that the cluster names a capability, with the priority the judgement
sets, or that it does not and the eighteen stay as they are for a reason
somebody wrote down. `typo3-content-element-development` already exists, so the
first question is why it did not carry this session rather than what a new one
would say.
