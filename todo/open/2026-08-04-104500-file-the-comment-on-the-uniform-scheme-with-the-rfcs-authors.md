# File the comment on the uniform scheme with the RFC's authors

**Serves:** decisions/scope/
**Priority:** normal

`D-SCO-010` is the entry this answers for. Establish whether the public comment
period of
[the draft RFC on an MCP interface contract for TYPO3](https://gist.github.com/dkd-dobberkau/1f87ba4051fc85efbb9475d96babf1d5)
is open, and if it is, file
[the drafted comment](../../documentation/interface-contract/comment-on-the-uniform-scheme.md)
into it. On 2026-08-04 the gist carried one revision, created 2026-08-03, no
comments, and a Process History table dating only the row "Draft created" — the
row for the Public Comment Period had no date, so the period's opening is not
readable from the document and the card `todo/waiting/2026-08-03-150000` was
wrong to say it opened on 2026-08-03. Read the gist again for a second revision
and for a dated comment period row, and where the document still says nothing,
ask the initiator named in it rather than guessing. Filing is a gist comment or
whatever channel the revision names; what comes back goes into `D-SCO-010` as a
**Since then**, and where the answer names a scheme, the rename of
`typo3://core` it implies is a todo of its own. It is `normal` rather than
`high` because the contract targets Q1 2027 and nothing here breaks until a
scheme is allocated, and not `low` because `D-SCO-010`'s whole bet is that this
comment gets filed before that happens.
