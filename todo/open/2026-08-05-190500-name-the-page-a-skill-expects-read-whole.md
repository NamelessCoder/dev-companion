# Name the page a skill expects read whole

**Serves:** feedback/2026-08-05-034022-this-is-the-what-i-never-put-to-the-server-half.md
**Priority:** normal

A session that arrives through a skill never learns the corpus exists: the
`instructions` name the index inside a sentence about `typo3_server_scope`, and
a session holding an order of calls has no reason to ask for orientation. Start
with `typo3-core-patch-development`, whose push step is the clearest case — it
sends the caller to `typo3_rule_lookup` for the Gerrit workflow, and the
procedure exists whole at `typo3://guides/core/contribution/gerrit-workflow`.
Name the uri at that step, then read every other skill for the same shape: a
step that describes a whole procedure and routes to a search for it. The second
half is the lookup answer — where what came back is one section of a document
served as a resource, say so and name the uri, since the excerpt reads as the
answer otherwise. Both halves are contracts, so the skill wording goes through
`SkillTest` and the answer change through the tool it belongs to; `D-AUD-007`
carries the reading and says why a `typo3_document_list` is not what this is.

Read what it serves and what the code does now before changing either; settle
what the step turns on rather than recalling it, and ask where nothing here can
answer: documentation/feedback/working-a-todo.md. Done means the file says so:
deleted, or trimmed to the part that is left.
