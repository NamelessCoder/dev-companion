# Take the condition off step 3 of the order, or say what else makes the call

**Serves:** decisions/
**Priority:** normal

`D-SKL-015`'s third **Wrong if** has fired twice — a session that reached a
skill from its `description` skipped `typo3_task_guide` in
`feedback/2026-08-04-055715` before the condition was published and again on
2026-08-10, that time reading `skills/base.md` whole and loading the tool's
schema in the same batch as the four it went on to call (`D-SKL-033` names the
transcript). Read both sightings against step 3 as it stands, then either take
its condition out — in `skills/base.md` and in `R-SKL-005`, which carries the
same wording in prose — or establish what would make the call happen where the
condition does not cover the session, and write which of the two into
`D-SKL-015` as a dated section. The priority is `normal` because two sessions
from two task shapes walked past the same step, not `low` as the card it came
from was.
