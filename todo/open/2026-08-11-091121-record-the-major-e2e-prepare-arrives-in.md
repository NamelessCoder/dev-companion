# Record the major `e2e-prepare` arrives in

**Serves:** knowledge/
**Priority:** normal
**Run:** bin/cli catalog:check

That command reads the `e2e-prepare` hint as declaring TYPO3 v13 and newer while
`Build/Scripts/runTests.sh` offers the suite on 14 and 15 only, so a 13.4 caller
is handed a command its own branch does not have. Re-read the script on all four
checkouts, record the range it says, and reread the sentence the hint carries
about the suite on both sides of that boundary. Until it is recorded the whole
command exits 1, which is the failure every other reading it makes now arrives
behind. Found on 2026-08-11 by a session cataloguing the module chrome, whose
subject this is not.
