# Nothing answers about an API at a TYPO3 major other than the installed one

**Serves:** feedback/2026-08-18-074124-nothing-answers-about-an-api-at-a-typo3-major.md
**Priority:** normal

Judged on 2026-08-18 as the ladder's step 1b and written up in `D-VER-007`. The
cost is real and the tool the feedback asks for is the one `doesNotCover`
refuses, which the same session reported as a strength half an hour later; the
changelog already reaches the other major and answers a different kind of
question there. What is missing is the procedure that names the reading.

Next step: write the `knowledge/documents/` page in the extension scope on
settling whether an API holds on a declared major that is not installed, read
against `.checkouts/13.4` and `.checkouts/14.3`, and route it so the question
reaches it. `D-VER-007` says what the page may state and what it may not.

`normal` because three feedback of that day stand on this boundary, one of them
as a strength, and because a session that skips the reading ships code that
fatals on a major its own manifest promises. All three came out of one
directory, which is what keeps it below work more than one project has reported.
