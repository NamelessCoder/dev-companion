# Steps 2 and 5 of the base order were skipped and nothing noticed, on a task that changed code

**Serves:** feedback/2026-08-18-074327-steps-2-and-5-of-the-base-order-were-skipped.md
**Priority:** normal

Judged on 2026-08-18 as the ladder's step 2, delivery, and written up in
`D-GUI-013`: the deprecation sweep is stated only in step 5 of `skills/base.md`,
in the paragraph after the exemption that removes the step, and no brief the
caller is holding when the task changes shape names it. The re-runs behind that
are in the entry.

Two placements, and neither needs anything about TYPO3 established. In
`src/Tool/TaskGuide.php`, name the sweep where the change type produces a change
— the obligation and its axes, one call per declared major per tag, and not step
5's reasoning — and say in the brief for work that changes nothing that a re-run
would owe it, beside the diff, the coverage and the commit message it already
names. Then, in step 4 of `base.md`, name `omittedHints` as the data form of the
sentence that step sends the caller to read.
