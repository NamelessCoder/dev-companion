# A compound brief routes to every unit it names

**Serves:** feedback/2026-08-17-212538-no-skill-owns-the-project-as-a-deliverable-or.md
**Priority:** normal

Establish why `typo3_task_guide`, given a brief whose three units are a
development installation, a sitepackage with custom content elements and a
distribution carrying the content, names `content-element` alone and does not
name `installation-setup` even weakly — `D-SKL-050` holds the re-run to compare
against. Read `src/Tool/TaskGuide.php` for whether one intent is all the answer
can carry, and `knowledge/task-intents.json` for whether the match terms are the
whole of it; a change to either is measured by re-running that brief together
with a brief each of the other eighteen intents already matches, so a widened
match is not read as an improvement while it swallows its neighbours.
