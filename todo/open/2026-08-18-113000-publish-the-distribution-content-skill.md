# Publish the distribution content skill

**Serves:** feedback/2026-08-17-212538-no-skill-owns-the-project-as-a-deliverable-or.md
**Priority:** normal

`skills/typo3-distribution-content/` is written and declares itself a draft, so
no client has it and the feedback it answers is still open. What publication
owes is three things and one of them is not a session's to give: write the
contract case that states the task in a user's own words, run it in an
environment the skills are not installed into and record that run beside the one
with the skill — `D-SKL-035` — and show `SKILL.md` whole to whoever asked for
it, asking by name which step is missing, which is wrong, and what it claims
that is not true here. Publishing is then one commit: the `metadata` declaration
deleted, the intent that routes to it written in `knowledge/task-intents.json`,
the workflow named in `knowledge/server-scope.json`, and the room found in a
listing that stands at 3597 characters of 3600 — which is a reading of all
thirteen descriptions and is what `D-SKL-054` deferred to here.
