# Carry the activation measurement into the two entries it answers

**Serves:** D-SKL-033, D-SKL-013
**Priority:** high

Write the **Since then** both entries are owed, from a benchmark outside this
repository read on 2026-08-19. An equipped arm ran seventeen project tasks in
Claude Code 2.1.234 on `claude-opus-5`; the client's own init event lists all
twelve published skills beside the connected server, and `skills_used` is empty
on every row of all four recorded sweeps, eighty-two runs in total. One of the
seventeen called `typo3_task_guide`. It answered
`skills: ["typo3-content-element-development"]` and the line `D-SKL-013` put
above the payload, and the session went on through `typo3_schema_lookup`,
`typo3_changelog_lookup` and Bash without a `Skill` call. That is `D-SKL-013`'s
first **Wrong if**, where the route is read and no skill is loaded, and
`D-SKL-033`'s third, which counts sessions of that shape and names the project's
own agent instruction file as the channel to weigh once it is met. Weigh that
channel here rather than record it as untried a third time, and weigh it against
the other number the same run carries: `typo3_project_describe` was called
eleven times and `typo3_task_guide` once, so what the `instructions` reach is
their first sentence and not their second. The run is somebody else's and is
left unnamed on purpose, so what may be cited by name is the author's to give
and is asked for before either entry claims a source.
