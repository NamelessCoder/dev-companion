# Reach the session that never asked about the borrowed class

**Serves:** feedback/2026-08-19-090231-a-borrowed-backend-css-class-shipped-unverified.md
**Priority:** normal

The catalog half is built and settled. `classesSince` is derived on every entry,
`coveredClasses` answers a class the query named outright on a target its entry
is withheld for, and `D-CAT-006` records the shape. Asked on 2026-08-21 whether
that range stays on the class list or goes down to one class each, the
maintainer answered the class list: a range per class would answer the v12
caller this feedback came from, and it costs 120 class-to-version pairs across
21 entries, written and re-derived by hand on every core release. It goes finer
when a second feedback asks about a class below its list's binding, and until
then `D-CAT-006` carries the measurement that says which callers are not reached
— the table class list binds at v13, so a v12 caller is still told nothing.

What is left is the entry point, which is why the feedback stays open. The
session's own account is that the task announced itself as npm dependency
maintenance and no instruction attached to a task reached it, so it never made
the call the catalog would have answered. That is `D-SKL-067`'s subject rather
than the catalog's, and `skills/typo3-extension-asset-build/` is the draft
written against it — so the first step is to read that draft against this
session's account and say whether it would have reached it.
