# Go back to the nine open decisions of July

**Serves:** decisions/
**Priority:** normal

Read each entry's **Wrong if** against what the repository does today and give
it the dated section that reading earns: **Confirmed on** where the statement
survives it, **Revoked on** with a `revokedBy` where it does not, and neither
where only a forward run or an outside event could answer — that third case
stays open and says in **Since then** what would settle it. The nine are
`D-CAT-001`, `D-GUI-001`, `D-SCO-005` and `D-SCO-006` of 2026-07-29, `D-CAT-003`
and `D-KNW-004` of 2026-07-30, and `D-ANS-004`, `D-EVI-003` and `D-FBK-004` of
2026-07-31. `D-ANS-004` is the third case before it is read:
`todo/waiting/2026-08-19-090401` is blocked on a measurement of the same 2048
characters, and the maintainer answered on 2026-08-21 that it is measured before
anything is designed against it — so what this entry gets is a **Since then**
naming that run, not a **Confirmed on**. July is the batch because it is the
part of `decisions/` the repository has moved furthest from, and because a
finishable batch is what the queue can hold: finishing this deletes the file,
`bin/cli unresolved:list` stops seeing a queued todo serving `decisions/`, and
`judge what nothing has answered for` asks the next session to queue the next
batch. How a dated section is written and what `confirmed` may be claimed of is
[documentation/records/writing-a-decision.rst](../../documentation/records/writing-a-decision.rst).
