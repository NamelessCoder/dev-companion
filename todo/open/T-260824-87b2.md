---
serves: [feedback/2026-08-24-173211-the-guides-list-was-returned-and-never-pulled.md]
priority: normal
---

# the guides list was returned and never pulled on; I hand-assembled the changelog rules instead

Archive this feedback in the commit that ships the guide reference's `when`
line, which [`T-260824-cecc`](T-260824-cecc.md) carries. Nothing here is a
repair to make on its own: the reading of 2026-08-25 found the pointer this
feedback asks for already built and already firing for its task shape, the brief
returns the same two rule sections either way, and the changelog half is
answered by `core/contribution/changelog` being the one match its own query
returns. What was left was the question of whether a page is handed over whole
rather than named, answered on 2026-08-27 as the line instead.
