# The patch skill says what a widening re-establishes

**Serves:** feedback/2026-08-24-225243-both-patch-skills-handle-a-linear-task-this-one.md
**Priority:** normal

Write into `skills/typo3-core-patch-development/SKILL.md` what a session does
when the request widens after the patch is under way: say again what the change
is, which branches it reaches and what it owes, and which of the three moved.
`D-SKL-079` is the judgement — step 4, wording — and carries the session that
re-derived those four times while both rules that would have carried it sat in
the file bound to the assessment.

Two placements are open and the body decides between them: under "Keep the patch
one change", where `D-SKL-075`'s paragraph already stands and where a session
decides what this patch is, or at the foot of "Make the change", where a
widening is actually met. The three things named are the skill's own — the
change type from step 2, the branches from the blast-radius paragraph, the entry
from its own section — so point at them rather than restate them.

Then hold it with a `SkillTest` sentence over the body, and write the
requirement the demand is: `R-SKL-027` is what the neighbouring rule in the same
section became. `D-KNW-127` answered the report's other half, which is why the
feedback is trimmed rather than archived.
