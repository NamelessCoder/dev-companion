---
serves: [feedback/2026-08-24-163321-the-repository-s-own-agents-md-routes-agents.md]
priority: normal
---

# The repository's own AGENTS.md routes agents around a tool that answers better

Write the patch to `typo3/AGENTS.md`, answered that way on 2026-08-27 against
waiting for this server to stop being experimental. It is one line in that
file's Context list: `typo3_forge_lookup` where the server is available, with
the `curl` recipe kept as the fallback for a session without it. The change goes
through Gerrit under the maintainer's own name, so the push is theirs rather
than a session's — prepare it the way `skills/typo3-core-patch-development`
prescribes and ask before it leaves this machine. Everything the patch owes
beyond the line is the core's own process, which that skill and
`typo3_task_guide` already carry, and
[`D-AUD-013`](../../decisions/audience/aud-013-a-competing-route-is-corrected-where-it-is-written.md)'s
third **Wrong if** is what the outcome is read against.
