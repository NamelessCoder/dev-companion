---
serves: [feedback/2026-08-24-140421-five-tools-i-loaded-or-was-pointed-at-and-never.md]
priority: normal
---

# Five tools I loaded or was pointed at and never called, and the one schema I guessed wrong

Have `typo3_project_describe` volunteer the deprecated-files verdict for the
extensions that are the project's own, answered that way on 2026-08-27 against
naming the call that carries it instead. The field is `deprecatedFiles`, built
by
[`D-ANS-009`](../../decisions/answers/ans-009-a-shipped-file-deprecation-is-found-by-the-tool-that-lists-the-file.md)
and computed in `Extension::deprecatedFiles()` from the registration files that
answer already lists — which the project answer does not list, so where the
verdict sits in it is what to settle first. That entry's second **Wrong if** is
what either shape has to be built against: an answer that volunteers
deprecations being read as a compatibility verdict. Nothing else in this
feedback is left, because the reading of 2026-08-25 found
`ExtensionDescribe::description()` complete and in context under a deferring
client, with the call still not made.
