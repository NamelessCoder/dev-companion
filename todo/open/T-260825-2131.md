---
serves: [feedback/2026-08-25-114819-the-resource-list-was-never-shown-to-the-client.md]
priority: normal
---

# The commit guide names the page its whole subject is written up in

Judged on 2026-08-27 as the ladder's step 2, delivery, and the feedback is
trimmed to that half. The reading is under the entry of 2026-08-27 in
[`D-ANS-061`](../../decisions/answers/ans-061-an-answer-that-names-a-document-hands-it-over.md),
which is also where the two answered halves are re-read.

`normal` rather than `low`, set by four sessions wanting
`core/contribution/commit-messages` and none reading it. One of them was
standing in this tool's answer when it gave up and assembled the page by hand.

The step: have `typo3_commit_message_guide` name that page on the core answer,
as the call that reads it — `typo3_rule_lookup` with `documentId`, never the
`typo3://guides` address, which is what `R-ANS-028` demands and why. The core
answer rather than both, because the page describes the core repository alone
and says so in its own `whenToUse`. `CommitMessageGuideTest` holds it, declaring
`R-ANS-028`.

What the reading established, so it is not done again. The page is named in
`TaskIntents::RULE_DOCUMENTS`, in `RuleLookup`'s `documentId` description as the
worked example, in the `Commit messages` routing entry as a `typo3://guides`
address, and in `typo3-core-patch-development` as a step — four surfaces, none
of which this session passed. `D-GUI-020` inlined one of the page's facts into
the same answer on 2026-08-26; inlining the rest is the second **Wrong if** of
`D-ANS-061`, so what is left to add is the pointer.
