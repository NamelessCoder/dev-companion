---
serves: [feedback/2026-08-25-114802-no-skill-activated-typo3-core-patch-development.md]
priority: normal
---

# Route a task in the core's own build scripts to the core's own workflow

Judged as the ladder's step 3 and measured rather than read: `typo3_task_guide`
with `paths: ["Build/Scripts/runTests.sh"]` answers `scope: "core"` and names
`typo3-extension-testing`, where the same brief with a `typo3/sysext/` path
names `typo3-core-patch-development` — `D-SKL-080` has the readings and what was
declined. Make `Scope::isCoreWork()` answer true for a path `Scope::CORE_LAYOUT`
already places in the core, either by giving `CORE_WORK` those two path markers
or by having the gate read the scopes `TaskGuide` has already computed for the
call, and hold it with a test that the two briefs above name the same workflow.
Read `Scope::CORE_ONLY_ARTIFACTS` in the same pass: it carries `runtests.sh` and
`build/scripts/` where `CORE_WORK` carries no core path but `typo3/sysext/`.
