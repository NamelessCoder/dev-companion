---
serves: [feedback/2026-08-25-114735-the-mandated-opening-calls-project-describe-and.md]
priority: normal
---

# The commit draft names the workflow the commit belongs to

Judged on 2026-08-27 as step 2, delivery: the imperative to call
`typo3_task_guide` arrived, is quoted back in the report word for word, and lost
to the session's own harness prompt, while the one answer that session did ask
for — `typo3_commit_message_guide`, in the turn the work became a core patch —
names nothing onward. `D-ANS-117` is the judgement, the boundary it decided and
what would show it wrong.

Next step: have `typo3_commit_message_guide` name `typo3_task_guide` and the
workflow it was asked with, in its answer. Whether the pointer is prose, a field
on the output schema or both is what `R-ANS-002` decides, and the schema is why
this was queued rather than made in the judging run.

The `typo3_test_run_guide` half is answered and trimmed off the feedback; the
report's note that the same tool never fired across eight dispatcher runs is a
second instance of this failure rather than separate work.
