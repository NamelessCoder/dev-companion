---
serves: [feedback/2026-08-25-114714-typo3-forge-lookup-lost-to-the-raw-curl-recipe.md]
priority: normal
---

# typo3_forge_lookup lost to the raw curl recipe that the core's AGENTS.md documents

Judged on 2026-08-27 as the ladder's step 4, wording, and recorded in
[`D-AUD-014`](../../decisions/audience/aud-014-a-description-opens-with-what-the-callers-own-route-cannot-do.md);
it is queued rather than done because the change is in `src/`. Open
`ForgeLookup::description()` in `src/Tool/ForgeLookup.php` and put what a
hand-written request to the tracker cannot do into its first sentence: this
passes the bot protection the core's own `AGENTS.md` warns about, and it answers
a tracker that did not reply differently from a search that matched nothing.
Both are true of the tool already — the second is the description's last
sentence and the first is only in the message a refused caller gets — so the
step moves a claim to where the call is decided rather than making a new one.
