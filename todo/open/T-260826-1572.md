---
serves: [feedback/2026-08-26-223414-four-things-in-the-forge-tool-that-carried-the.md]
priority: normal
---

# Say what a row's ABANDONED still costs a call

Step 4, wording, and the evidence is `D-ANS-069`'s first **Wrong if** firing on
the first session to hold the field it was written for. That entry carries the
reading, and this is the change it queued.

`ForgeLookup::workflow()` opens its ordered readings with *what has already
happened to it, which `typo3_gerrit_lookup` answers by the number before the
checkout is opened. An abandoned change is a verdict somebody wrote down*, under
an instruction to stop at the first that decides. The block was written when
that state cost the call it names. The row carries the state now, so the reading
looks answered by the page, and the argument on the change — which the paragraph
above the rows says an `ABANDONED` is grounds to read — is what the skipped call
would have supplied. Say in the bullet what the row already carries and what
still costs a call. `skills/typo3-core-issue-triage/SKILL.md` states the same
sentence at step 7 with the diff behind it, so what the block needs is the half
the compression dropped rather than a new claim.

The second sentence is in the same file and is what misplaced item 3 of the
report. The `notes` description invites the call with *ask for it when sweeping
candidates*, and says which ways in ignore it in its last sentence; sweeping
candidates is what that session called the enumeration, which ignores it. Name
the reading — one issue at a time — or put the scope ahead of the invitation.

Both are `src/`, which is why neither was made in the judging run.
