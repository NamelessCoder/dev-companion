---
serves: [feedback/2026-08-27-145349-open-stale-plus-category-and-the-comments-it.md]
priority: low
---

# open="stale" plus category and the comments it carries killed three candidates without opening them

Put the canonical "find me something to work on" call into
`typo3_forge_lookup`'s description as a worked example: `open="stale"` with a
`tracker` and a `category`, which is the combination the reporting session
assembled by reading the parameter prose through. The judgement is step 4,
wording, and it touches `src/`, which is what keeps it off the spot. It stays
`low` because one session asked and the description is already among the longest
this server hands a client — so the example replaces the prose it saves rather
than being added under it, and `bin/cli prose:check` is what says whether it
did. What the same feedback reports as working is judged and gone: the strength
is evidence about the boundary
[`D-ANS-069`](../../decisions/answers/ans-069-a-backlog-row-carries-the-review-server-and-not-the-journal.md)
drew, written into that entry under 2026-08-27.
