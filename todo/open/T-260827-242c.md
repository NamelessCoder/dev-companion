---
serves: [feedback/2026-08-27-145332-server-instruction-block-pre-empted-the-triage.md]
priority: normal
---

# Server instruction block pre-empted the triage and patch skills; neither activated

Name `typo3-core-patch-development` in a brief's `skills` from the declared
`changeType` and the core paths, where the intents name nothing —
[`D-SKL-082`](../../decisions/task-skills/skl-082-a-declared-change-type-on-core-paths-names-the-patch-workflow.md)
has what was measured and what would show it wrong. The route half is already
answered and gone: `in forge` and `on forge` joined the tracker needles, so the
session's own sentence now names both core workflows in order. What is left is
the shape the needles cannot reach — a task worded as the defect rather than as
the work, which answers `skills: []` with `changeType="bugfix"` on a
`typo3/sysext/` path. It is `normal` because one session reported it and the
emptiness is in the most obviously covered task shape rather than at the edge,
and it touches `src/`, which is what keeps it off the spot. The feedback's other
half — that the `instructions` block routes past the skills before they are read
— waits on [`T-260819-dcaf`](../waiting/T-260819-dcaf.md): the sentence it asks
for costs about seventy characters against the twenty `D-AUD-011` measured free,
so nothing is designed against that budget until the measurement comes back.
