# Task: TYPO3 extension conformance audit. Tool call sequence and round trips:

**Serves:** feedback/2026-07-31-193005-task-typo3-extension-conformance-audit-tool.md
**Priority:** low
**Branch:** todo/task-typo3-extension-conformance-audit-tool
**Claimed:** 2026-08-02
**Waiting on:** is a session's own log of its tool calls the event the
    `D-SKL-001` **Wrong if** describes — the base outrun — and if it is, what
    moves? Three answers, and none of them may be taken here. The decision
    stands with the cost written into it, which is what the new **Since then**
    already leaves it as. Or the order stops being prose that is read once and
    becomes an answer the server hands back, which means an audit intent in
    `knowledge/task-intents.json` so that base step 3 stops returning a patch
    checklist to a review — a tool's answer, so it is queued rather than
    improvised. Or the run is recorded rather than reasoned about, which is a
    forward `REVIEW-02` in `site-new` on a model this repository has never
    measured, and that costs an environment somebody owns. The recommendation is
    the second with the third behind it: the intent gap is a fact of the server
    today, and it is the only one of the three a session here can work.

The judgement is made and written. It is on
[`D-SKL-001`](../../decisions/task-skills/skl-001-the-order-a-task-starts-in-is-one-file.md)
in a **Since then** — ladder step 5, a feedback against a decision's **Wrong
if** — and on
[`D-FBK-018`](../../decisions/feedback/fbk-018-a-strength-is-evidence-about-a-boundary-not-about-a-decision.md),
whose first **Wrong if** it is: a strength that carries a lever nothing else
does, so it was walked down the ladder rather than closed as boundary-evidence.
What the re-run established from `site-new` on 2026-08-02 is in both entries and
is not repeated here — the server's half of the run reproduces, and step 3 of
the base is the one that would not have paid. The feedback stays open because
nothing was changed for it, and it is archived by whatever change the answer
above turns out to be.
