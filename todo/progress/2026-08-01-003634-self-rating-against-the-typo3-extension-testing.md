# Debrief of the TYPO3 14 testimonials session: self-rating against the typo3-extension-testing ski...

**Serves:** feedback/2026-08-01-003634-self-rating-against-the-typo3-extension-testing.md
**Priority:** low
**Branch:** todo/self-rating-against-the-typo3-extension-testing
**Claimed:** 2026-08-02
**Waiting on:** the question `2026-08-01-002951` and `2026-08-01-003103` wait on,
    and one answer settles all three: may a summary feedback be archived once
    every half it names is carried by a named sibling, or does it stay open
    until the last of those siblings lands? This is the third of the series and
    the weakest case for archiving on its own — no half of it has landed, and
    one is queued as work — but it is the strongest case for the sixth answer
    `2026-08-01-002951` recommends, archiving on supersession with the commit
    naming the siblings, because a fourth summary of one session would otherwise
    cost a fourth mapping. Putting this back unanswered is one of the answers.

Judged, and the judgement is at the foot of
[`D-FBK-021`](../../decisions/feedback/fbk-021-a-summary-feedback-is-judged-against-its-series-not-on-its-own.md):
the third summary of the series, and the first that is a self-rating rather than
a roundup — five sections scored against the testing skill's own workflow.
Mapped rather than walked down the ladder: the order and the declared commands
are `003356` under `D-SKL-001`, the browser layer is `003533` under `D-KNW-017`
with its card in `todo/open/`, the ViewHelper tests are `003933`, the debug left
in the test is `003938`, and the "never activated" premise is withdrawn by
`003736`. No half is orphaned, so no todo is derived and nothing about TYPO3 was
read here. What the strengths add is where the boundary runs: the functional
layer was right for the defect below the UI, and every cost sits on the other
side of that line, which is what the crossing `D-KNW-017` queues must reach
without pulling the below-the-UI case with it.
