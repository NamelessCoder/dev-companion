# Say what a client still needs before the entry just written can be called

**Serves:** R-DIS-023, feedback/2026-07-31-185900-during-an-audit-of-the-printworks-3d-site.md
**Priority:** normal
**Branch:** todo/say-what-a-client-still-needs-before-the-entry-just-written-can-be-called
**Claimed:** 2026-08-02

Step 2 of the ladder, delivery: the server answers and the entry is correct, and
two sessions in `site-new` still had no callable tool — `D-DIS-009` has the
re-run and both sightings. Establish first, per client `Installer` supports,
what stands between a written `.mcp.json` and a tool in the session: whether it
scopes project servers behind an approval, and whether a session already open
picks the file up. That is a question about clients this repository does not
own, so it is read from each client's own documentation at the version in play,
and a client whose behaviour cannot be established is recorded as unknown rather
than guessed at. Then put what comes back into the output of `install` and
`update`, beside the line that reports the entry written, and into
`documentation/clients/installing.md` next to the `chat.useAgentSkills` note it
already carries for VS Code skills.
