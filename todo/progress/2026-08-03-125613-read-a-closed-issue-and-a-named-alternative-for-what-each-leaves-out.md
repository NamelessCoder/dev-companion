# Read a closed issue and a named alternative for what each leaves out

**Serves:** feedback/2026-08-02-144800-task-assess-forge-105403-and-fix-it-the-user.md
**Priority:** normal
**Branch:** todo/read-a-closed-issue-and-a-named-alternative-for-what-each-leaves-out
**Claimed:** 2026-08-03
**Waiting on:** the wording is on the branch and needs the reading
    `writing-a-skill.md` asks for by name, because a published skill is a copy
    no release of this server corrects. Does step 3 of "Establish the issue
    before you believe it" match how a closed Forge issue is really read — is
    *write out what the alternative drops against what the reported code did,
    argument by argument* the step that would have turned the session behind
    `feedback/2026-08-02-144800` at its first assessment rather than its third?
    Which of the two readings is wrong, and what does either claim that is not
    true of the work? "Does this look good?" is what that page says not to ask,
    so this is asked instead. Putting the todo back is one of the answers.

The two readings are written, as acts with objects, inside the step rather than
as a section of their own: the closure reason and the target version are read
for what the conversation decided and said back, and a comment that names an
alternative is answered by naming the arguments and the behaviour the reported
code had and the replacement does not. `D-SKL-009` is what the shape is held to.
`SkillTest::aClosedIssueIsReadForWhatTheConversationDecided` holds both, their
place inside the section, and that they stand ahead of the reproduction.

What came out where it grew is the step's own tail — *what the reporter saw,
what the branch does today, and what the project intends the API to be for* —
which restated the step's opening and step 4. The other candidate was measured
and kept: "Keep the patch one change" duplicates *Keep changes focused on one
bug, feature, cleanup, or test improvement* in
`knowledge/documents/typo3-core-rules.md`, but `typo3_rule_lookup`, re-run on
2026-08-03 through `bin/typo3-cms-mcp`, reaches that section only from a query
naming *contribution flow*. `bugfix`, `bugfix patch obligations` and
`what a bugfix patch must contain` all miss it, so cutting the paragraph would
put the rule behind a query this skill does not make.

The feedback stays open behind this claim until the wording is read. Nothing
else of it is left: the two readings above were the whole of what it still
asked, and archiving it is the answer's last step rather than this one's.
