# Hold a manual result to the coverage the score already computes

**Serves:** feedback/2026-08-03-164734-installation-the-extension-ships-a-fork-of-the.md
**Priority:** normal
**Branch:** todo/hold-a-manual-result-to-the-coverage-the-score-already-computes
**Claimed:** 2026-08-03

Step 5 of the ladder rather than 4: `D-ANS-021`'s first **Wrong if** fired — the
match line was delivered, read, and did not save the call — and the lever that
entry named is refuted, because two of the three queries have no page body to
match. Give `Documentation::lookup()` the floor `Documents::search()` applies:
`TermSearch::score()` returns the coverage as its second value and `lookup()`
discards it in the destructuring, so divide it by the query's asked-for weight
and drop what falls below. Set the value against the live 14.3 index and the
`DocumentationTest` fixtures together — 0.5 empties the two queries that no page
answers, keeps `record objects` and `login provider` at 1.00, and leaves one
collision at 0.60 on the third. Where nothing clears it the answer is `empty`
rather than the best six, and it carries what a miss owes elsewhere:
`Search\Subsets::largestReaching()` over the same searchable fields and
`TermSearch::carries()`, then the corpus sentence `D-ANS-043` built, pointing at
`typo3_changelog_lookup` — which returns `Feature-108166` alone for `fluid file
extension`, the entry this session read `TemplatePaths::resolveFileInPaths()` to
reconstruct. What must hold from then on is written beside `R-DOC-002`, and the
commit that ships it archives the feedback.
