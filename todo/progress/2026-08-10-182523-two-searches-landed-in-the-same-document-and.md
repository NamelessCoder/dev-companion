# two searches landed in the same document and neither answer suggested reading it whole

**Serves:** feedback/2026-08-10-182523-two-searches-landed-in-the-same-document-and.md
**Priority:** normal
**Waiting on:** Whether the answer side of `D-ANS-070` gets a third attempt,
    whether the round trip is removed instead of advertised, or whether the cost
    is accepted. Nothing here decides it: both levers that entry built were in
    the session and neither took, so what is left is what a further attempt is
    worth, which is a judgement rather than a reading.
**Branch:** todo/two-searches-landed-in-the-same-document-and
**Claimed:** 2026-08-11

Judged on 2026-08-11 as **step 5**: the feedback satisfies the first **Wrong
if** of
[`D-ANS-070`](../../decisions/answers/ans-070-a-document-is-handed-over-by-the-call-that-reads-it-and-by-what-the-answer-left-of-it.md),
where the re-run and the dating are written. What it asks for is the line it
already received. It is the first report on this ground since both halves of
that entry landed, which is what puts it above `low`.

The three answers and what each costs:

- **Accept it.** Nothing changes, and the answer goes on saying what the search
  left of the page. The next session on this ground reports the same miss.
- **A third wording attempt.** Say the position at the head of the answer rather
  than the subtraction at its foot — *Changelog Files is 1 of the 10 sections of
  core/contribution/commit-messages* — above the excerpt a reader stops in. That
  is `src/Result/Prose.php` and the two `KnowledgeTest` methods `D-ANS-070`
  names, and it rests on one session's own wording of what it missed.
- **Remove the round trip.** Where a search matches one document, answer with
  the document. The whole page costs one call, which the two searches had
  already spent, and it cuts across `D-ANS-061` and `R-ANS-028`, which made the
  second call the handover.

The third is the recommendation, because it stops asking a session to act on a
sentence, and two sessions have now been handed one and not acted on it.
