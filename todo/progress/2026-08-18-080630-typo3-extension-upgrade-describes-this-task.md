# typo3-extension-upgrade describes this task almost word for word and stayed shut, because the req...

**Serves:** feedback/2026-08-18-080630-typo3-extension-upgrade-describes-this-task.md
**Priority:** normal
**Branch:** todo/typo3-extension-upgrade-describes-this-task
**Claimed:** 2026-08-18

Judged on 2026-08-18 as the ladder's step 3 and written up in `D-SKL-061`: the
words that would have matched are in the description already, and the clause
they are read under — carrying a package from the versions it supports today to
another set — states a premise a bug report inside a declared range does not
meet. `knowledge/task-intents.json` is shut on the same shape, its two intents
matching on `deprecat` and `breaking` and carrying the core author's checklist.

Rewrite the premise so the removed and deprecated surface of a major is a case
of its own, and move the body with it: `SKILL.md` opens "Cross a package from
the range it declares to the range it is meant to declare" and resolves that
range in step 3, so a description widened alone disagrees with the file it sits
in. What the third step becomes where nothing is being crossed is the question
the rewrite has to answer.

The listing had 28 characters left over the ceiling on the day this was judged,
so the rewrite trades words rather than adding a shape — the arithmetic is
`D-SKL-026` and the measurement is in the decision. Hold the pair the way
`SkillTest::aWorktreeTaskMatchesTheSkillThatOwnsTheCheckout` holds the one it
was measured on, so the next trim cannot take the trigger back without the body
noticing.

`feedback/2026-08-18-081159-skills-are-matched-once-against-the-opening.md` is
the same session on why the description had only one chance to fire, and keeps
its own card; the proving-the-other-major half of this feedback was trimmed to
`feedback/2026-08-18-081129`, which owns it with the procedure it would have
needed.
