# The installation procedure is a guide a caller can read

**Serves:** feedback/2026-08-18-070538-no-guide-covers-booting-an-installation-and-the.md
**Priority:** normal
**Branch:** todo/the-installation-procedure-is-a-guide-a-caller
**Claimed:** 2026-08-18

`D-KNW-095` judged this step 1b and took it on: every fact is written, and the
procedure sits in the hint corpus while the guides list is what a caller chooses
a procedure from. Settle first whether the two branches are one document or two,
by reading `installation-boot`, `installation-setup` and the two sections
`skills/typo3-development-installation/SKILL.md` forks into — the skill forking
on them is the reason to expect two. Then write it below
`knowledge/documents/project/installation/` with the front matter `D-KNW-057`
requires, moving the ordered steps out of `installation-boot` rather than
copying them and leaving that entry its facts, and name it from the skill at the
step in the shape `D-SKL-045` set. Start after `D-SKL-056` has re-cut that fork
and `D-ANS-085` has made the list visible on a fresh clone, since a guide the
reader cannot see there is half delivered.
