# Re-cut the installation skill's fork and what it owes at the end

**Serves:** feedback/2026-08-18-070448-the-installation-skill-has-two-shapes-and-this.md
**Priority:** normal
**Branch:** todo/re-cut-the-installation-skills-fork-and-its
**Claimed:** 2026-08-18

Rewrite two places in `skills/typo3-development-installation/SKILL.md` on
`D-SKL-056`, and move the assertions in
`SkillTest::anInstallationIsBuiltInDependencyOrderAndHandsOverOnceItAnswers` with
them, which hold the section headings verbatim. The fork above **Boot what the
repository already declares** branches on whether the repository declares a boot
procedure rather than on the traces an installation leaves, and says that one
declaring an environment and no procedure runs what is declared and takes the
rest from the create branch without changing anything declared. The closing
section splits on what the run wrote into the repository rather than on who wrote
the sequence, so a run whose every written path is ignored owes the site
answering and a report saying why the unattended re-run and the commit message
had no subject. No third section: `D-SKL-052` is what the file's size is paid
with. The commit archives the feedback with `bin/cli feedback:archive`.
