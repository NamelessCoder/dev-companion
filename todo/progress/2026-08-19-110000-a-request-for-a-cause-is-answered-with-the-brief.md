# A request for a cause is answered with the brief for writing a patch

**Serves:** D-SKL-065
**Priority:** normal
**Branch:** todo/a-request-for-a-cause-is-answered-with-the-brief
**Claimed:** 2026-08-21

Give `knowledge/task-intents.json` the fourth entry that changes nothing, beside
`audit`, `triage` and `installation-operations`: a defect reported by its
symptom, where the request is the file and the cause and nothing is to be
changed yet. `D-SKL-065` carries what was measured — a content element rendering
wrong hands over the workflow for adding one, a blank backend module the one for
building one, and a page answering with an error is recognized as nothing and
comes back with keep the change focused, add coverage, draft the commit message.
Where the intent routes is the question the needles decide: the installation
half is `typo3-development-installation`'s, and a cause in the package's own
code has no workflow before the fix is agreed. Measure the needles against the
corpus and against the cases that have to keep missing, the way `D-SKL-039` did,
and check `TaskGuide`'s fork afterwards — an intent recognized beside
`content-element` still has to lose the patch skeleton. `SKILL-15` is the case
this is judged by.
