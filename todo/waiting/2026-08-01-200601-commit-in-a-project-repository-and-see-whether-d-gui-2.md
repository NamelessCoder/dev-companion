# Commit in a project repository and see whether `D-GUI-002` holds

**Serves:** decisions/
**Waiting on:** may a session be run in one of the `E-EXT` checkouts that
    changes code and commits it, so the first half of the `D-GUI-002`
    **Wrong if** can be watched rather than reasoned about? Nothing produces
    that run today: the three forward reviews are reviews, `D-EVI-003` says a
    review changes nothing, and `EXT-03` is a contract case, which is read
    rather than run forward. So either a fourth forward review is written whose
    task ends in a commit, or the run is driven ad hoc and reported as a
    feedback. Both are a change to how this repository takes evidence, and the
    checkouts belong to somebody.

The half that could be settled here is settled, and it is recorded on
`D-GUI-002` in a **Since then** section: the call without the argument really
does answer with the hard `missing-issue` error, and the four channels that
could name `workflow="project"` were read one by one. The follow-up call
`typo3_task_guide` prints outside the core now carries the argument, held by
`ScopeTest::theBriefPointsAtTheGuideForTheStepItEndsWith`, and `EXT-03` says
which half of it nothing guards. What is left is the run, and one thing the
reading turned up is worth taking into it: none of the seven published skills
names `typo3_commit_message_guide` at all, so a session that arrives through a
skill has no route to this step — which would make the run's answer a property
of that hole rather than of the argument's default.
