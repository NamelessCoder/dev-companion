# Ask whether a skill lists the tools it ends in for a client that defers schemas

**Serves:** feedback/2026-08-18-074627-every-tool-arrives-deferred-so-each-first-use.md
**Priority:** low
**Waiting on:** whether a skill names the tools its workflow ends in together
    near its top, so a client that fetches a schema per first call can batch
    them. Judged on 2026-08-18 as the ladder's step 5 and written up in
    `D-SKL-060`: every name was delivered, in the file the session was working
    from and before the call that wanted it, and what the feedback measures is
    what naming a tool at the step that needs it costs such a client. The
    recommendation is not to, because the line is a second copy of the routing
    in a file no release of this server corrects, because the discharge rule
    already gives a second mention another meaning, and because the one surface
    a deferring session gets whole is the `instructions`. What is asked is
    whether to pay that anyway.

Nothing is built until the question comes back. The reading behind it is in
`D-SKL-060`, and the two things that would start work are its first two **Wrong
if**: a session that read the `instructions` and still paid a fetch per first
call, or one that reports batching off a list at the top of a skill.

The other half of the client property is not this card's. `D-AUD-003` decided
that the `instructions` carry what the tool descriptions cannot deliver, and
`feedback/2026-08-18-113308` reports a session that called nothing at all under
the same deferral and asks for what to call for what in that block. Whether the
`instructions` gain such an index is that feedback's judgement, and it was
deliberately not made here.
