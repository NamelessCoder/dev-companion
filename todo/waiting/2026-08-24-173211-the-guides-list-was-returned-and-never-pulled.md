# the guides list was returned and never pulled on; I hand-assembled the changelog rules instead

**Serves:** feedback/2026-08-24-173211-the-guides-list-was-returned-and-never-pulled.md
**Priority:** normal
**Waiting on:** whether a guide is handed over whole rather than named. The card
    serving `feedback/2026-08-24-140239` carries that question already, and this
    feedback is one of the reports it is asked on, so what comes back answers
    both. `documentation/records/judging.rst` step 5 — a change to what every
    answer weighs, which that page says is never made quietly. The reading is
    under the entry of 2026-08-24 in
    [`D-GUI-012`](../../decisions/guides/gui-012-the-brief-names-the-guide-the-recognized-work-belongs-to.md).

Judged on 2026-08-25 and the feedback is trimmed to the half above. Its
changelog half is answered and closed by re-running the session's own query:
`typo3_rule_lookup` returns `core/contribution/changelog` as its one match and
names the `documentation-changelog` hint beside it, and every byte-level item
the session counted is in that hint. The reading, including the
include-directive claim refuted against `.checkouts/main`, is under the entry of
2026-08-25 in
[`D-KNW-111`](../../decisions/knowledge/knw-111-the-changelog-procedure-is-a-guide-of-its-own.md).

**Priority:** `normal` rather than `low`, set by the question being the one
another judged card is already blocked on rather than by this report alone.

What the reading established about the half that is left, so it is not done
again. The feedback's first suggestion — that `typo3_task_guide` name the guide
covering the task it was asked about — is built and it fires for this task
shape: the `changelog` intent names `typo3_rule_lookup` with the `documentId`
first in its `nextTools`, with what the page holds. Its `guides` field stays
empty on core work, and the reason recorded for that no longer describes the
corpus, which is corrected under the entry of 2026-08-25 in `D-GUI-012`. The
shape is right there anyway: a second pointer to one page in one answer is that
entry's third **Wrong if**. So the pointer this feedback asks for exists,
arrives with the work rather than at orientation, and was still not pulled on —
which is the question above and not a lever this repository can pull on its own.

The next step once it is answered: nothing here is a repair to make first. The
brief was measured against the corpus before the changelog split and returns the
same two rule sections either way, so the one change that suggests itself —
searching the new page in `TaskIntents::RULE_DOCUMENTS` — was run in this
worktree and moved nothing.
