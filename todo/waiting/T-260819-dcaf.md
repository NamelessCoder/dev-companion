---
serves: [feedback/2026-08-19-090401-tools-arrived-as-bare-names-with-no-schemas-and.md]
priority: low
waitingOn: >
  whether 2048 characters is still the instruction budget to design against.
  Judged on 2026-08-21 as the ladder's step 5 and written up in `D-AUD-011`,
  whose first **Wrong if** this feedback satisfies: a session under a deferring
  client read the six-entry index and called nothing at all. What it asks for
  instead is a statement of the boundary, and every lever left costs room the
  block does not have — 2028 characters of the 2048 in the longest assembly,
  twenty free against about seventy for one bullet. The one `D-AUD-011` named as
  what was left to try, `D-SKL-060`'s fetch line, was refused on 2026-08-19.
  `D-ANS-004` took 2048 from one client's release runs on 2026-07-31 and nothing
  has re-measured it since; the client reporting here is another one, which
  delivered the block whole. What is asked is whether to have that measured
  before designing further against 2048, or to accept the boundary statement as
  unaffordable and leave this feedback on the design's price. Put to the
  maintainer on 2026-08-21, and the answer was to measure first: nothing is
  designed against 2048 until one session reports the length of the
  `instructions` its client actually kept, which is the hand-started run
  described at the foot of this card. Put again on 2026-08-27, and the
  maintainer took that run: what this waits on is the measurement itself rather
  than a decision about it.
---

# Tools arrived as bare names with no schemas, and the document list was never seen at all

Nothing is built until the question comes back, and the priority stays `low`
because the two costs the session's wrong boundary judgement actually produced
are on their own cards — `feedback/2026-08-19-090231`, a backend CSS class
borrowed unverified, and `feedback/2026-08-19-090253`, a commit message derived
from `git log`.

## What the judgement established, 2026-08-21

By re-running the calls the session did not make, rather than from the report:

- The line the feedback asks for already exists, and the session had it. The
  index entry naming the call that reads a whole procedure landed in `d8acbbf0`
  ten hours before the feedback was filed, and the session still reports
  finishing without learning the corpus can be enumerated. So nothing is
  trimmed: the suggestion is answered in form and unanswered in fact, which is
  the same step-5 finding as the rest.
- `typo3_reference_list` is not the document list. It enumerates the core's own
  worked examples — `theme_camino`, `styleguide` — so naming it in the block
  would not have shown the session the shelf it says it missed.
- `typo3_script_lookup`, asked the session's own subject
  (`npm build scripts webpack in an extension`), answers with the four topics of
  the core script notes and routes elsewhere. The name did mislead it; the call
  the name cost would have returned nothing about the task.
- Renaming that tool is refused where `D-AUD-011` already refuses it: a tool
  name is the one surface clients installed months ago have written down.

What would unblock the question is one session reporting the length of the
`instructions` its client actually kept. It has to be started by hand in a
current client, the way `todo/waiting/2026-08-05-183000` does — nothing here can
drive one, because every session that could measure it runs elsewhere.
