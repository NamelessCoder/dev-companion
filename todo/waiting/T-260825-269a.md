---
serves: [feedback/2026-08-25-105231-no-route-for-extending-somebody-else-s-patch.md]
priority: normal
waitingOn: >
  The skill half of this change is written and not yet reviewed. Does
  `typo3-core-patch-checkout`'s fourth way in match how extending somebody
  else's Gerrit change is really done here — the ask before the upload, local
  work committed on its own branch first rather than stashed, the file by file
  transcription rule, and the ending where the checkout stays on the review
  branch and hands over to `typo3-core-patch-development`? Which step is
  missing, which one is wrong, and what does it claim that is not true in this
  project? Read `skills/typo3-core-patch-checkout/SKILL.md` and its
  `references/checklist.md` whole on this branch.
---

# Show the patch set route's skill half and work the answer in

The reading and the writing are done on `todo/T-260825-269a`, and what is left
is the review `documentation/contributing/writing-a-skill.rst` asks for before a
skill is published — which here is before this branch is merged, since
`Installer::skills()` publishes whatever the directory holds. `D-KNW-129` now
carries what the reading settled: the contribution guide answers a foreign
change with a patch set and asks for the author's leave first, Gerrit grants
`Add Patch Set` to registered users by default, and a scratch clone with the
core's own `commit-msg` hook showed the amend keeping the author, moving the
committer, leaving the `Change-Id` single and unchanged and the branch one
commit above its base. The mechanics are in
`knowledge/documents/core/contribution/gerrit-workflow.md` under **Open a Patch
Set on Somebody Else's Change**, held by
`KnowledgeTest::aPatchSetOnSomebodyElsesChangeSaysWhatItOwesThatAuthor`. Ask the
question in the front matter above, work what comes back into the two skill
files, then archive the feedback with `bin/cli feedback:archive` in the same
commit and delete this file.
