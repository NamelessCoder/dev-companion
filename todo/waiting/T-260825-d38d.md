---
serves: [feedback/2026-08-25-105324-task-guide-returned-a-large-brief-that-changed.md]
priority: low
waitingOn: >
  Does a second session report a brief whose items did not bear on a mechanical
  change — a link swap, a label rewording, a formatting pass? `D-GUI-022`
  declines to build a change type for it on one report. Read again on
  2026-08-27, `bin/cli feedback:list` holds the nearest thing yet and it is not
  the same shape: `feedback/2026-08-27-145507` reports a bugfix checklist that
  did not scale down to a three-statement patch, which changes behaviour. Its
  own card judges it, and that judgement is what says whether this gate fired.
---

# typo3_task_guide takes a change type for a mechanical sweep

The other half is done: `typo3_test_run_guide` names the paths that reached no
suite, as `uncoveredPaths` and as a sentence beside the narrowing one, which is
`R-ANS-036` and the **Since then** of `D-GUI-022`.

**The step, gated.** Add a `changeType` for a sweep that changes no behaviour —
a checklist arm beside the four of `D-GUI-008` and `D-SKL-065`, being prove
every replacement, name the file types no suite covers, keep the diff to the one
thing, and say what was left out. It is `low` because one session asked for it
and `D-GUI-022` priced what that buys; the gate above is what raises it, so read
`bin/cli feedback:list` for a second report before writing anything.
