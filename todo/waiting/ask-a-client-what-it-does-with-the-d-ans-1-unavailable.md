# Ask a client what it does with the `D-ANS-1` unavailable shape

**Serves:** decisions/
**Waiting on:** may a directory be designated `E-NONE`, and may `E-SITE` be
    stopped to play `E-STOPPED`, so `META-02` can be run forward as a session
    rather than read as a contract? The reading this todo asked for is done and
    it settles nothing: no recorded run and no feedback has ever met the
    unavailable shape, because every one of them came from a directory whose
    installation was reachable. Only a session in a directory that has none can
    show what a client does with it, and nothing on this machine plays that.
    The alternative is to leave the **Wrong if** unobserved until a client hits
    it by accident, which is how the 2026-07-29 evidence arrived.

What is left is the run, and the reading behind it is recorded on `D-ANS-1` in
a **Since then** line. Once a directory plays `E-NONE`, run `META-02` there as
a forward session — its prompt asks for a "publish" icon and a "Publish page"
label, and both reach installation-answered lookups — then record whether the
answer says the installation could not be asked or reports that no such icon
and no such label exist. The second is the **Wrong if** firing, and
`isError: true` is the lever the entry already names for it. Do not re-derive
what the corpus holds: it holds nothing, and `D-ANS-1` says why.
